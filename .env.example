MEDIA_PROXY_SECRET=replace-with-a-long-random-secret
# (Optional) Proxy URL to route server-side requests (useful if Vercel serverless IP gets blocked by APIs)
# Example: PROXY_URL=http://username:password@proxy-domain:port
# PROXY_URL=
